/**
 * front() // returns the first element
 * back() // returns the last
 * clear() // removes all elements
 * empty() // returns true if arr is empty
 * 
 * 
 * 
 * you can't cout a vector or iterator value like that
 */